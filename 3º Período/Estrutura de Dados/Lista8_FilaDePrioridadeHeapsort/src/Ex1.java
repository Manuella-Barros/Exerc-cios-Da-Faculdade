public class Ex1 {
    public static void main(String[] args) {
        // Heap é uma das maneiras de guardar e acessar elementos com a complexidade O(log n)
        // O heap guarda em um vetor os elementos com uma organização que simula a estrutura de uma arvore
        // Por utilizar o conceito de arvores completas, ir completando de cima pra baixo da esquerda pra direita,
        // qualquer movimentação na arvore custa no máximo O(log n)

        // Exemplo heap minimo -> [ A, B, C, D, E, F, G ]
        //            A
        //         B     C
        //       D  E   F  G
    }
}
